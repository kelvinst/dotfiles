# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking

# Config version for compatibility and deprecations
# Fallback value (if you omit the key): config-version = 1
config-version = 2

# You can use it to add commands that run after AeroSpace startup.
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
after-startup-command = [
  'exec-and-forget borders active_color=0xffe1e3e4 inactive_color=0xff494d64 width=5.0'
]

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 300

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# Fallback value (if you omit the key): on-focused-monitor-changed = []
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# You can effectively turn off macOS "Hide application" (cmd-h) feature by toggling this flag
# Useful if you don't use this macOS feature, but accidentally hit cmd-h or cmd-alt-h key
# Also see: https://nikitabobko.github.io/AeroSpace/goodies#disable-hide-app
automatically-unhide-macos-hidden-apps = true

# List of workspaces that should stay alive even when they contain no windows,
# even when they are invisible.
# This config version is only available since 'config-version = 2'
# Fallback value (if you omit the key): persistent-workspaces = []
persistent-workspaces = []

# A callback that runs every time binding mode changes
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# See: https://nikitabobko.github.io/AeroSpace/commands#mode
on-mode-changed = []

# Environment variables for exec-and-forget commands.
# See: https://nikitabobko.github.io/AeroSpace/guide#exec-env-vars
[exec]
    inherit-env-vars = true
[exec.env-vars]
    PATH = '/Users/kelvinstinghen/.local/bin:/opt/homebrew/bin:/opt/homebrew/sbin:${PATH}'

# Possible values: (qwerty|dvorak|colemak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
[key-mapping]
    preset = 'qwerty'

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See:
#                 https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
    inner.horizontal = 12
    inner.vertical =   12
    outer.left =       12
    outer.bottom =     12
    outer.top =        12
    outer.right =      12

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
# Fallback value (if you omit the key): mode.main.binding = {}
[mode.main.binding]

    # All possible keys:
    # - Letters.        a, b, c, ..., z
    # - Numbers.        0, 1, 2, ..., 9
    # - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
    # - F-keys.         f1, f2, ..., f20
    # - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon,
    #                   backtick, leftSquareBracket, rightSquareBracket, space, enter, esc,
    #                   backspace, tab, pageUp, pageDown, home, end, forwardDelete,
    #                   sectionSign (ISO keyboards only, european keyboards only)
    # - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
    #                   keypadMinus, keypadMultiply, keypadPlus
    # - Arrows.         left, down, up, right

    # All possible modifiers: cmd, alt, ctrl, shift

    # All possible commands: https://nikitabobko.github.io/AeroSpace/commands

    # See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
    # You can uncomment the following lines to open up terminal with alt + enter shortcut
    # (like in i3)
    # alt-enter = '''exec-and-forget osascript -e '
    # tell application "Terminal"
    #     do script
    #     activate
    # end tell'
    # '''

    # Disabling macOS "Hide application" feature, because I don't use it
    # See: https://nikitabobko.github.io/AeroSpace/goodies#disable-hide-app
    cmd-h = [] # Disable "hide application"
    cmd-alt-h = [] # Disable "hide others"

    # See: https://nikitabobko.github.io/AeroSpace/commands#layout
    alt-slash = 'layout tiles horizontal vertical'
    alt-f = 'layout accordion horizontal vertical'

    alt-q = 'exec-and-forget orbit preset --main-size +150 2-col-left'
    alt-w = 'exec-and-forget orbit preset --main-size +150 2-col-right'
    alt-shift-q = 'exec-and-forget orbit preset --main-size +50 2-row-top'
    alt-shift-w = 'exec-and-forget orbit preset --main-size +50 2-row-bottom'

    alt-a = 'exec-and-forget orbit preset --main-size +150 3-col-left'
    alt-s = 'exec-and-forget orbit preset --main-size +150 3-col-middle'
    alt-d = 'exec-and-forget orbit preset --main-size +150 3-col-right'
    alt-shift-a = 'exec-and-forget orbit preset --main-size +50 3-row-top'
    alt-shift-s = 'exec-and-forget orbit preset --main-size +50 3-row-middle'
    alt-shift-d = 'exec-and-forget orbit preset --main-size +50 3-row-bottom'

    alt-t = 'layout floating tiling' # Toggle between floating and tiling layout

    # Fullscreen
    alt-z = 'fullscreen'

    # Reload stuff
    alt-b = 'balance-sizes'
    alt-r = 'flatten-workspace-tree' # reset layout
    alt-shift-r = ['reload-config', '''exec-and-forget osascript -e 'display notification "Config reloaded" with title "AeroSpace"' ''']

    # See: https://nikitabobko.github.io/AeroSpace/commands#focus
    alt-h = 'exec-and-forget orbit focus --boundaries-action sibling-workspace left'
    alt-j = 'exec-and-forget orbit focus --boundaries-action sibling-workspace down'
    alt-k = 'exec-and-forget orbit focus --boundaries-action sibling-workspace up'
    alt-l = 'exec-and-forget orbit focus --boundaries-action sibling-workspace right'

    # See: https://nikitabobko.github.io/AeroSpace/commands#move
    alt-shift-h = 'exec-and-forget orbit swap-or-move-node-to-workspace left'
    alt-shift-j = 'exec-and-forget orbit swap-or-move-node-to-workspace down'
    alt-shift-k = 'exec-and-forget orbit swap-or-move-node-to-workspace up'
    alt-shift-l = 'exec-and-forget orbit swap-or-move-node-to-workspace right'

    alt-shift-ctrl-h = 'exec-and-forget orbit move --boundaries-action sibling-workspace left'
    alt-shift-ctrl-j = 'exec-and-forget orbit move --boundaries-action sibling-workspace down'
    alt-shift-ctrl-k = 'exec-and-forget orbit move --boundaries-action sibling-workspace up'
    alt-shift-ctrl-l = 'exec-and-forget orbit move --boundaries-action sibling-workspace right'

    alt-ctrl-h = 'join-with left'
    alt-ctrl-j = 'join-with down'
    alt-ctrl-k = 'join-with up'
    alt-ctrl-l = 'join-with right'

    # See: https://nikitabobko.github.io/AeroSpace/commands#resize
    alt-minus = 'resize smart -50'
    alt-equal = 'resize smart +50'

    # See: https://nikitabobko.github.io/AeroSpace/commands#workspace
    alt-1 = 'workspace 1'
    alt-2 = 'workspace 2'
    alt-3 = 'workspace 3'
    alt-4 = 'workspace 4'
    alt-5 = 'workspace 5'
    alt-6 = 'workspace 6'
    alt-7 = 'workspace 7'
    alt-8 = 'workspace 8'
    alt-9 = 'workspace 9'
    alt-0 = 'workspace 0'

    # See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
    alt-shift-1 = 'move-node-to-workspace 1 --focus-follows-window'
    alt-shift-2 = 'move-node-to-workspace 2 --focus-follows-window'
    alt-shift-3 = 'move-node-to-workspace 3 --focus-follows-window'
    alt-shift-4 = 'move-node-to-workspace 4 --focus-follows-window'
    alt-shift-5 = 'move-node-to-workspace 5 --focus-follows-window'
    alt-shift-6 = 'move-node-to-workspace 6 --focus-follows-window'
    alt-shift-7 = 'move-node-to-workspace 7 --focus-follows-window'
    alt-shift-8 = 'move-node-to-workspace 8 --focus-follows-window'
    alt-shift-9 = 'move-node-to-workspace 9 --focus-follows-window'
    alt-shift-0 = 'move-node-to-workspace 0 --focus-follows-window'

    alt-ctrl-1 = 'summon-workspace 1'
    alt-ctrl-2 = 'summon-workspace 2'
    alt-ctrl-3 = 'summon-workspace 3'
    alt-ctrl-4 = 'summon-workspace 4'
    alt-ctrl-5 = 'summon-workspace 5'
    alt-ctrl-6 = 'summon-workspace 6'
    alt-ctrl-7 = 'summon-workspace 7'
    alt-ctrl-8 = 'summon-workspace 8'
    alt-ctrl-9 = 'summon-workspace 9'
    alt-ctrl-0 = 'summon-workspace 0'

    alt-period = 'workspace --wrap-around next'
    alt-comma = 'workspace --wrap-around prev'

    alt-tab = 'move-workspace-to-monitor --wrap-around next'
    alt-shift-tab = 'move-workspace-to-monitor --wrap-around prev'

    # Opening applications
    cmd-ctrl-enter = 'exec-and-forget orbit open net.kovidgoyal.kitty'
    cmd-ctrl-1 = 'exec-and-forget orbit open -a com.1password.1password'
    cmd-ctrl-comma = 'exec-and-forget orbit open -a com.apple.systempreferences'
    cmd-ctrl-a = 'exec-and-forget orbit open -a com.apple.iCal'
    cmd-ctrl-b = 'exec-and-forget orbit open com.google.Chrome'
    cmd-ctrl-f = 'exec-and-forget orbit open com.apple.finder'
    cmd-ctrl-k = 'exec-and-forget orbit open -a io.zsa.keymapp'
    cmd-ctrl-m = 'exec-and-forget orbit open -a com.apple.mail'
    cmd-ctrl-n = 'exec-and-forget orbit open -a com.apple.Notes'
    cmd-ctrl-p = 'exec-and-forget orbit open -a com.apple.Music'
    cmd-ctrl-w = 'exec-and-forget orbit open -a net.whatsapp.WhatsApp'
    cmd-ctrl-z = 'exec-and-forget orbit open -a us.zoom.xos'

    # See: https://nikitabobko.github.io/AeroSpace/commands#mode
    alt-semicolon = 'mode service'

# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
    esc = ['reload-config', 'mode main']
    r = ['flatten-workspace-tree', 'mode main'] # reset layout
    f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout
    backspace = ['close-all-windows-but-current', 'mode main']

    # sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
    #s = ['layout sticky tiling', 'mode main']

# Assign workspaces to their monitors.
# See: https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[workspace-to-monitor-force-assignment]
    0 = 'secondary'
    1 = 'main'

[[on-window-detected]]
    if.app-id = 'com.apple.iCal'
    check-further-callbacks = true
    run = ['move-node-to-workspace 0']

[[on-window-detected]]
    if.app-id = 'io.zsa.keymapp'
    check-further-callbacks = true
    run = ['move-node-to-workspace 0']

[[on-window-detected]]
    if.app-id = 'com.apple.mail'
    check-further-callbacks = true
    run = ['move-node-to-workspace 0']

[[on-window-detected]]
    if.app-id = 'com.apple.Notes'
    check-further-callbacks = true
    run = ['move-node-to-workspace 0']

[[on-window-detected]]
    if.app-id = 'com.apple.Music'
    check-further-callbacks = true
    run = ['move-node-to-workspace 0', 'layout tiling']

[[on-window-detected]]
    if.app-id = 'net.whatsapp.WhatsApp'
    check-further-callbacks = true
    run = ['move-node-to-workspace 0']

[[on-window-detected]]
    if.app-id = 'us.zoom.xos'
    check-further-callbacks = true
    run = ['move-node-to-workspace 0']

