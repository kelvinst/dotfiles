[init]
  defaultBranch = main
[user]
  email = kelvin.stinghen@me.com
  name = kelvinst
[core]
  editor = nvim
  excludesfile = ~/.global_gitignore
  pager = delta
[diff]
  tool = nvim -d
[merge]
  tool = nvim -d
[difftool]
  prompt = false
[filter "lfs"]
  clean = git-lfs clean -- %f
  smudge = git-lfs smudge -- %f
  process = git-lfs filter-process
  required = true
